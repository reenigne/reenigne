Stall in configuration 774430
  Glitch or real?
  Could something have overflowed?
  Add ability to dump state so that it can be restored once we've got some debugging equipment in place
  ~100 streams per second - only about 26 of the bars are giving data (on average)

Programmer
  Write back backup OSCCAL to PIC (0xC24)
  Write code to automatically test speed:
    Output over serial: OSCCAL value, mean and std dev

Buy connectors and resistors

When the hardware doesn't work:
  Get something simpler working
  Run timing tests (make sure MOVWF PCL takes one cycle)

